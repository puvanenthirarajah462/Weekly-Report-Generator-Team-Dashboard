DJANGO_SECRET_KEY=change-me
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Set DB_ENGINE=sqlite to skip MySQL setup during quick local testing
DB_ENGINE=mysql
DB_NAME=weekly_reports
DB_USER=root
DB_PASSWORD=
DB_HOST=127.0.0.1
DB_PORT=3306

CORS_ALLOWED_ORIGINS=http://localhost:3000

# Optional — only needed for the AI Chat Assistant feature
GEMINI_API_KEY=
